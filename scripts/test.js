test('fng', function(){
	ok(fng, 'fng is ready');
});

test( "fng grid", function() {
  ok( fng.grid, "grid is ready" );
});

test( "fng chart", function() {
  ok( fng.chart, "chart is ready" );
});

test( "fng layer", function() {
  ok( fng.layer, "layer is ready" );
});

test('fng validator', function(){
	ok( fng.validator, 'validator is ready');
});

test('fng ajax', function(){
	ok( fng.ajax, 'ajax is ready');
});

test('fng util', function(){
	ok( fng.util, 'util is ready');
});

test('fng debug', function(){
	ok( fng.debug, 'util is debug');
});